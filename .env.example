GEMINI_API_KEY=...

# Optional locally (falls back to an in-process Map when unset). Required in
# production so uploaded documents survive across serverless instances.
# Provisioned automatically by Vercel's Redis Marketplace integration.
KV_REST_API_URL=...
KV_REST_API_TOKEN=...
